#pragma once
#include <string>

/// Se ha centralizado todos los IDs aquí para no crear lo mismo todo el rato

enum BallId {
    NORMAL_BALL,
    ABBACUS,
    BOWLING,
    CRISTAL,
    QUANTIC,
    PETANQUE,
    POKEBALL,
    X2,
    NUM_BALLS
};

enum StickId {
    NORMAL_STICK,
    BOXING,
    DONUT,
    GRENADE,
    WAND,
    NUM_STICKS
};