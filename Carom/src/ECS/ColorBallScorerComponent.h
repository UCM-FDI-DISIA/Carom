#pragma once
#include "Component.h"

//cambiar todo esto con herencia del componente de Mika
class ColorBallScorerComponent : ecs::Component{
    /*
        ON HIT ENTER
        if(isColorBall) ColorHitManager.checkColisions(pos de hit);
    */
};